\documentclass[10pt]{article}

\usepackage{neuripsstyle}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,bm}
\usepackage{booktabs}
\usepackage{array,multirow}
\usepackage{longtable}
\usepackage{graphicx}
\usepackage{adjustbox}
\usepackage{siunitx}
\usepackage{float}
\usepackage{placeins}
\usepackage{pdflscape}
\usepackage{caption}
\usepackage[hidelinks]{hyperref}
\usepackage{microtype}
\usepackage{titlesec}
% formatting-only tightening to respect the 9-page main-body limit (no content changes)
\titlespacing*{\section}{0pt}{4pt plus 1pt minus 1pt}{2pt plus 1pt minus 1pt}
\titlespacing*{\subsection}{0pt}{3pt plus 1pt minus 1pt}{2pt plus 1pt minus 1pt}
\titlespacing*{\paragraph}{0pt}{2pt plus 1pt minus 1pt}{0.6em}
\captionsetup{font=small,labelfont=bf,skip=2pt,belowskip=0pt}
\renewcommand{\arraystretch}{1.0}
\graphicspath{{figs/}}
% --- compact spacing (formatting only, no content change) to respect the 9-page main-body limit ---
\AtBeginDocument{%
  \setlength{\textfloatsep}{3pt plus 2pt minus 2pt}%
  \setlength{\floatsep}{3pt plus 2pt minus 2pt}%
  \setlength{\intextsep}{3pt plus 2pt minus 2pt}%
  \setlength{\abovedisplayskip}{3pt plus 1pt minus 1pt}%
  \setlength{\belowdisplayskip}{3pt plus 1pt minus 1pt}%
  \setlength{\abovedisplayshortskip}{1pt plus 1pt}%
  \setlength{\belowdisplayshortskip}{1pt plus 1pt}%
}

% --- notation ---
\newcommand{\zeq}{z^{\mathrm{eq}}_t}
\newcommand{\zbd}{z^{\mathrm{bd}}_t}
\newcommand{\Reg}{\mathcal{S}}
\newcommand{\Path}{\mathcal{P}}
\newcommand{\Acoup}{\textsc{coupled}}
\newcommand{\Bindep}{\textsc{independent}}
\sisetup{detect-weight=true,detect-family=true}

\title{Regime-Aware Tactical Asset Allocation for Equities and Fixed Income:\\
Independent and Coupled Jump-Model Labels}

\author{%
Team RLMM\\
\normalsize Machine Learning for Finance and Complex Systems}
\date{\today}

\begin{document}
\maketitle
\thispagestyle{empty}


\begin{abstract}
    \noindent We study whether forecasted equity and bond return regimes improve monthly allocation across equities, bonds, and cash after transaction costs. The empirical design estimates realised regimes, forecasts next-month regimes, and maps forecast probabilities into portfolio weights. Unsupervised jump-model teachers estimate equity and bond return environments from realised return-based features. Student models are supervised only by these teacher labels and forecast next-month regimes from lagged macro-financial predictors; the \Acoup{} student also uses current-regime indicators and regime run length. The resulting forecast probabilities are converted into regime-conditional mean--variance allocations.

    \noindent We compare two regime representations. The \Bindep{} specification estimates separate two-state favourable and unfavourable labels for equities and bonds. The \Acoup{} specification estimates a three-state joint stock--bond label with calm, normal, and stressed regimes. Both representations distinguish the central portfolio mechanism: bonds support equity drawdowns in disinflationary episodes such as the dot-com bust and the global financial crisis, while inflation-driven sell-offs such as 1994 and 2022 weaken the bond hedge. In the student stage, regularised logistic regression and random forests produce informative next-month regime probabilities. In the allocation stage, selected dynamic strategies improve on the 60/40 benchmark along distinct margins, including lower drawdowns, higher Sharpe ratios, and higher terminal wealth, depending on risk aversion and turnover control.
\end{abstract}


\section{Introduction}

Balanced portfolios depend on how equity and bond returns interact across market environments. In disinflationary equity sell-offs, bond returns often rise and reduce portfolio losses. In inflation-driven sell-offs, equities and bonds can both lose value. A tactical allocation rule therefore needs information about the prevailing stock--bond environment when choosing next-month equity, bond, and cash exposures.

This paper studies monthly regime-aware allocation for a broad equity leg and a broad fixed-income leg. The equity leg represents the stock component of a balanced portfolio, and the bond leg represents the fixed-income component. The empirical design has three stages. First, unsupervised jump-model teachers estimate regime labels from equity and bond returns derived features.
Second, student models forecast the next-month teacher labels from lagged macro-financial
predictors; the \Acoup{} student also uses current-regime indicators and regime run length. Third, the allocation stage maps
the forecast probabilities into regime-conditional return moments and monthly equity, bond,
and cash weights.

We compare two regime-label structures. The \Bindep{} approach estimates separate binary
favourable-state labels for the equity and bond legs. The \Acoup{} approach estimates one
joint stock--bond label with calm, normal, and stressed states. The independent structure
gives asset-level signals; the coupled structure gives a direct signal for the joint
diversification environment.

The rest of the paper follows the same sequence. Section~\ref{sec:data} describes the return
series and predictors. Section~\ref{sec:teacher} reports the realised regime labels.
Section~\ref{sec:student} reports next-month forecast performance. Section~\ref{sec:alloc}
evaluates the allocation results. The appendices report formulas, grids, diagnostics, and
sensitivity checks.

\section{Data}
\label{sec:data}

The empirical analysis uses monthly excess returns for the equity and bond legs; cash is the zero-excess-return asset in the allocation stage.
The equity leg is the S\&P~500. The bond leg is the Bloomberg aggregate bond-index.
The one-month risk-free rate defines excess returns,
\[
    r^{\mathrm{eq}}_t=\text{equity return}_t-rf_t,\qquad
    r^{\mathrm{bd}}_t=\text{bond return}_t-rf_t .
\]

\paragraph{Teacher features.}
The \Bindep{} equity teacher uses daily equity exponentially weighted returns and Sortino
ratios over 21, 63, 126, and 252 trading days. The \Bindep{} bond teacher uses monthly bond
exponentially weighted returns, downside risk, Sharpe ratios, and volatility over one- to
twelve-month horizons. The \Acoup{} teacher uses monthly equity and bond Sortino, downside-deviation,
and trend features. Appendix~\ref{app:features} reports the formulas, horizons, and
transformations.

\paragraph{Student panel.}
The student panels are monthly and lagged. The \Bindep{} panel combines equity, Treasury,
credit, commodity, volatility, macro, sentiment, and stock--bond correlation predictors,
with causal winsorisation, rolling z-scores, and exponential smoothing. The \Acoup{} panel
uses a screened monthly predictor set for the joint equity--bond label: lagged macro-financial,
Treasury, commodity, and regime-context variables, together with current-regime indicators
and regime run length. Appendices~\ref{app:features} and~\ref{app:student-coupled} report
the predictor definitions and transformations.

\section{Teacher: regime labeling with statistical jump models}
\label{sec:teacher}

The teacher stage defines the realised regimes that the student models later forecast. The
jump-model teachers are unsupervised: they estimate regimes from return-based features. A regime label must be persistent and must
separate different return outcomes. Persistence measures whether a state represents a
sustained environment. State-conditional returns show whether the states distinguish
favourable from adverse outcomes. Crisis-window diagnostics compare the labels with known
stock--bond episodes.


\subsection{The jump model}

A jump model assigns each observation to a finite set of states while penalising frequent state changes. For a standardised feature vector \(x_t\), state \(s_t\), and state centres \(\{c_k\}\), the state sequence solves
\begin{equation}
    \min_{s_1,\dots,s_T,\,\{c_k\}}\
    \sum_{t=1}^{T} d(x_t,c_{s_t})
    +
    \lambda\sum_{t=2}^{T}\Lambda(s_{t-1},s_t),
    \label{eq:jump-generic}
\end{equation}
where \(d(\cdot,c_k)\) measures distance from state centre \(c_k\), \(\Lambda\) penalises switches, and \(\lambda\) controls persistence. Higher values of \(\lambda\) produce longer spells. Using an iterative approach, the model uses dynamic programming to find the state sequence from the current centers, then updates the centers based on that sequence. To avoid local minima, this loop is run from several different initializations.

\subsection{Two regime parameterisations}
\label{sec:two-parameterisations}

While both regime specifications rely on the jump-model penalty, they define distinct state spaces. Both teachers are fit causally; thus, the label for month $t$ is derived strictly from observations available at or before that point.

\paragraph{Independent: two independent binary labels.}
The \Bindep{} teacher estimates separate two-state jump models for the equity and bond legs. For asset \(a\in\{\mathrm{eq},\mathrm{bd}\}\), feature vector \(x^a_i\), and teacher member \(p\), the state sequence solves
\begin{equation}
    \min_{s^a_1,\dots,s^a_{T_a},\,\{c^a_0,c^a_1\}}
    \sum_{i=1}^{T_a} d_p\!\left(x^a_i,c^a_{s^a_i}\right)
    +\lambda_p\sum_{i=2}^{T_a}\mathbf{1}\{s^a_i\neq s^a_{i-1}\}.
    \label{eq:independent-teacher}
\end{equation}
The equity teacher relies on daily observations, whereas the bond teacher uses monthly data. Both models employ an identical 12-year historical training window before starting and update at each month-end. During these updates, features are standardized using only the training sample available up to that specific date.

The teacher ensemble aggregates models across a grid of persistence penalties ($\lambda_p$) using both $L^2$ (squared Euclidean loss with mean centers) and $L^1$ (absolute loss with medoid centers) specifications. Appendix~\ref{app:teacher-independent} details the parameter grids and ensemble construction.

After each fit, hidden states are ranked by their realized in-sample Sharpe ratio, and the higher-Sharpe state ($g^{a,p}$) is labeled ``favorable.'' Because the equity teacher runs on daily data while the bond teacher runs on monthly data, their monthly target labels are defined as:
\begin{equation}
    S^{\mathrm{eq},p}_m
    =
    \mathbf{1}\!\left\{
    \frac{1}{n_m}\sum_{i\in m}\mathbf{1}\{s^{\mathrm{eq},p}_i=g^{\mathrm{eq},p}\}
    \ge \frac{1}{2}
    \right\},
    \qquad
    S^{\mathrm{bd},p}_m
    =
    \mathbf{1}\{s^{\mathrm{bd},p}_m=g^{\mathrm{bd},p}\}.
    \label{eq:independent-monthly-label}
\end{equation}
Thus, the monthly equity target is a majority vote of its daily states, whereas the bond target is simply that month's state.

\paragraph{\Acoup{}: three joint regimes with a coupling probability.}
The \Acoup{} approach represents the equity--bond environment with three joint regimes. Each
leg has a benign or adverse bit, and the admissible joint states are
\[
    \Reg=\{\,\text{calm}=(0,0),\ \ \text{normal}=(1,0),\ \ \text{stressed}=(1,1)\,\},
\]
where the pair is the equity bit and the bond bit. Calm means that both legs are benign; normal means that the equity leg is in its high-uncertainty state, positive on average but with the widest dispersion of the three regimes, spanning the disinflationary drawdowns such as the dot-com bust and the global financial crisis, while bonds remain benign and provide the hedge; and stressed means that both legs are genuinely adverse.

For a joint state $j$ with bits $(j_E,j_B)$, the per-month cost combines the two leg-specific
distances with a coupling term:
\begin{equation}
    e_t(j)=d\!\left(\zeq,\mu^{\mathrm{eq}}_{j_E}\right)+d\!\left(\zbd,\mu^{\mathrm{bd}}_{j_B}\right)
    +\eta\Big[\,\kappa_t\,\mathbf{1}\{j\in D\}+(1-\kappa_t)\,\mathbf{1}\{j\in A\}\,\Big],
    \label{eq:emission}
\end{equation}
where $A$ denotes the agree states, calm and stressed, and $D$ denotes the disagree state,
normal.
The scalar $\eta>0$ sets the strength of the coupling term, and $\kappa_t\in[0,1]$ is a learned
\emph{co-movement probability}: the model-implied probability that, given current market
conditions, the equity and bond legs occupy the same (agree) regime rather than the split
(disagree) regime. Because $\kappa_t$ multiplies the disagree state and $(1-\kappa_t)$ the agree
states in~\eqref{eq:emission}, a high $\kappa_t$ makes the agree states (calm, stressed) cheaper
and hence more likely, while a low $\kappa_t$ makes the disagree state (normal) cheaper; the
coupling term is thus a soft, data-driven prior on whether the two legs move together in
month~$t$.
Given the current state path, $\kappa_t$ is fitted by an
$L^2$-penalised logistic regression of the leg-agreement indicator
$y_t=\mathbf{1}\{s^{\mathrm{eq}}_t=s^{\mathrm{bd}}_t\}$ on equity--bond correlation, equity
and bond trend, and equity and bond downside deviation:
\begin{equation}
    \kappa_t=\sigma\!\left(\beta_0+\beta^{\top}x^{\mathrm{mkt}}_t\right),\qquad
    x^{\mathrm{mkt}}_t=\big\{\rho^{\,\mathrm{eq,bd}}_{12,t},\ \mathrm{trend}^{\mathrm{eq}}_t,\
    \mathrm{trend}^{\mathrm{bd}}_t,\ \mathrm{dd}^{\mathrm{eq}}_t,\ \mathrm{dd}^{\mathrm{bd}}_t\big\}.
    \label{eq:kappa}
\end{equation}
The transition penalty is applied separately to the equity and bond bits,
$\Lambda(i,j)=\lambda\,\mathbf{1}\{i_E\neq j_E\}+\lambda\,\mathbf{1}\{i_B\neq j_B\}$.
The coupling term acts as a soft bias in the state cost. It raises the cost of states that
conflict with the fitted equity--bond dependence signal and lowers the cost of states
supported by that signal.

The coupling logistic~\eqref{eq:kappa} is re-fitted at \emph{every} labeled month, with no
delay: at month~$t$ it uses only the expanding history through~$t$ (refit cadence one month),
and it is estimated \emph{jointly} with the state path inside the coordinate descent that
defines the teacher. Each iteration re-estimates the leg centres, refits the logistic of the
current leg-agreement indicator $y_t=\mathbf{1}\{s^{\mathrm{eq}}_t=s^{\mathrm{bd}}_t\}$ on
$x^{\mathrm{mkt}}_t$, and updates the path by dynamic programming under~\eqref{eq:emission}.
This also fixes the supervised/unsupervised status of the method: the jump model is
unsupervised, and although the coupling logistic is a supervised classifier, its target $y_t$
is generated by that same unsupervised model rather than by any external or forward-looking
label. The teacher stage is therefore unsupervised with respect to future returns and strictly
causal. Appendix~\ref{app:teacher-coupled} specifies the coupling logistic, its inputs, its
estimation, and its effect on the state cost in full.


\paragraph{Relationship.}
The two parameterisations define different regime objects. The \Bindep{} labels keep equity and bond regimes separate, so the final state can take any of the four favourable--unfavourable combinations. The \Acoup{} label is a single joint state. Calm corresponds to strongly favourable equity and favourable bonds; normal to uncertain equity (positive on average but spanning equity drawdowns) with favourable, hedging bonds; and stressed to unfavourable equity and unfavourable bonds. The \Bindep{} representation gives two asset-level regime signals. The \Acoup{} representation gives one regime signal for the joint equity--bond environment.
\subsection{Ensembling and Voting}
The final regime labels are aggregated across a grid of persistence penalties and specifications. For the \Bindep{} approach, equity and bond paths are combined independently via a majority vote across all constituent models. For the \Acoup{} approach, the ensemble aggregates 22 joint-regime specifications formed by crossing distinct feature families with the $L^1$ and $L^2$ loss configurations. In both settings, the resulting vote shares quantify model consensus. Appendix~\ref{app:teacher-independent} and Appendix~\ref{app:teacher-coupled} detail the explicit voting formulas, transition matrices, and model-level diagnostics.

\subsection{Label results}
\label{sec:teacher-results}

The two teacher specifications produce persistent regimes with distinct return profiles.
Figure~\ref{fig:labels} compares their label histories: the \Bindep{} panel marks
favourable-regime months for each leg, while the \Acoup{} panel shades the same stock--bond
history by calm, normal, and stressed joint regimes. Appendix~\ref{app:teacher-coupled}
reports coupled vote shares and the coupling-probability path.

\begin{figure}[H]
    \centering
    \makebox[\textwidth][c]{%
        \begin{minipage}[t]{0.60\textwidth}
            \centering
            \includegraphics[width=\linewidth]{teacher_regime_labels.png}
        \end{minipage}\hspace{0.025\textwidth}%
        \begin{minipage}[t]{0.60\textwidth}
            \centering
            \includegraphics[width=\linewidth]{COUPLED__labels.png}
        \end{minipage}}
    \caption{Causal teacher-label representations of the stock--bond history. Both panels report
        labels formed using information available through the labeled month. Left (\Bindep{}):
        equity and bond excess-return indexes with favourable-regime months marked for each leg.
        Right (\Acoup{}): cumulative excess-return indexes shaded by calm, normal, and stressed
        joint regimes.}
    \label{fig:labels}
\end{figure}

\paragraph{State economics.}
Table~\ref{tab:state-economics} reports label-conditional return statistics. In the
\Bindep{} panel, favourable states have positive Sharpe ratios and unfavourable states have
negative Sharpe ratios for both equity and bonds. In the \Acoup{} panel, calm is the strongest equity state; normal is the high-uncertainty equity state, positive on average but the widest-dispersion regime, and the one containing the disinflationary equity drawdowns, where bonds provide reliable support; and stressed records losses in both risky legs.

\begin{table}[H]
    \centering
    \caption{Label-conditional economics. The table reports same-month annualised excess return,
        annualised volatility, Sharpe ratio, sample share, and average spell duration. The
        \Bindep{} panel conditions on each leg's binary label. The \Acoup{} panel conditions on the
        three joint regimes. Panel headers report the causal label sample used for each
        representation.}
    \label{tab:state-economics}
    \scriptsize
    \setlength{\tabcolsep}{2.4pt}
    \renewcommand{\arraystretch}{0.96}
    \begin{adjustbox}{center,max width=1.22\textwidth}
        \begin{minipage}[t]{0.47\textwidth}
            \centering
             \textbf{Independent: two binary labels (1988--2024)}\\[-1pt]
            \begin{tabular}{@{}llrrrrr@{}}
                \toprule
                Class  & Regime       & Share  & Ret.      & Vol.     & Sh.     & Dur.           \\
                \midrule
                Equity & Favourable   & 76.3\% & $+13.8\%$ & $12.1\%$ & $+1.14$ & 14.3           \\
                Equity & Unfavourable & 23.7\% & $-15.4\%$ & $19.8\%$ & $-0.78$ & \phantom{0}4.6 \\
                Bond   & Favourable   & 67.1\% & $+5.7\%$  & $3.2\%$  & $+1.81$ & 13.8           \\
                Bond   & Unfavourable & 32.9\% & $-4.1\%$  & $5.1\%$  & $-0.81$ & \phantom{0}6.8 \\
                \bottomrule
            \end{tabular}
        \end{minipage}
        \hspace{0.025\textwidth}
        \begin{minipage}[t]{0.57\textwidth}
            \centering
            \textbf{Coupled: three joint regimes (1989-01 to 2024-11)}\\[-1pt]
            \begin{tabular}{@{}llrrrrr@{}}
                \toprule
                Class  & Regime   & Share            & Ret.      & Vol.     & Sh.     & Dur.           \\
                \midrule
                Equity & Calm     & 14.2\%           & $+21.0\%$ & $8.1\%$  & $+2.60$ & \phantom{0}4.7 \\
                Equity & Normal   & 76.6\%           & $+10.1\%$ & $14.9\%$ & $+0.68$ & 11.8           \\
                Equity & Stressed & \phantom{0}9.3\% & $-40.9\%$ & $13.7\%$ & $-2.99$ & \phantom{0}2.2 \\
                Bond   & Calm     & 14.2\%           & $+0.8\%$  & $3.5\%$  & $+0.22$ & \phantom{0}4.7 \\
                Bond   & Normal   & 76.6\%           & $+4.1\%$  & $3.9\%$  & $+1.06$ & 11.8           \\
                Bond   & Stressed & \phantom{0}9.3\% & $-8.4\%$  & $5.0\%$  & $-1.69$ & \phantom{0}2.2 \\
                \bottomrule
            \end{tabular}
        \end{minipage}
    \end{adjustbox}
\end{table}

\paragraph{Persistence.}
Persistent labels make the forecast horizon economically usable: a one-month-ahead signal has
allocation value only if regimes last long enough for portfolio weights to adjust without
being dominated by turnover costs. Table~\ref{tab:state-economics} reports average spell
durations, and Appendices~\ref{app:teacher-independent} and~\ref{app:teacher-coupled} report
the transition matrices and switch-count diagnostics.

\paragraph{Crisis windows.}
Table~\ref{tab:crisis} compares the labels across historical stress windows. The comparison
separates equity drawdowns with bond support from episodes in which equity and bond returns
fall together.

\begin{table}[H]
    \centering
    \caption{Crisis-window behaviour under both labelings. The \Bindep{} columns report the bond
        label's favourable share and Sharpe ratio. The \Acoup{} columns report the joint regime mix
        within each window and the equity and bond Sharpe ratios. Sharpe ratios use same-month
        annualised excess returns over each window.}
    \label{tab:crisis}
    \small
    \begin{adjustbox}{center,max width=\textwidth}
        \begin{tabular}{lrrrrrrr}
            \toprule
                                    & \multicolumn{2}{c}{\Bindep{} bond} & \multicolumn{3}{c}{\Acoup{} regime share} & \multicolumn{2}{c}{\Acoup{} Sharpe}                                               \\
            \cmidrule(lr){2-3}\cmidrule(lr){4-6}\cmidrule(lr){7-8}
            Window                  & Fav.                               & Sharpe                                    & Calm                                & Normal & Stress.        & Eq.     & Bd.     \\
            \midrule
            1990 recession          & 100.0\%                            & $+1.33$                                   & 0\%                                 & 100\%  & \phantom{0}0\% & $+0.05$ & $+1.41$ \\
            1994 bond shock         & \phantom{0}8.3\%                   & $-1.55$                                   & 8\%                                 & 33\%   & 58\%           & $-0.48$ & $-1.61$ \\
            Dot-com bust            & 87.5\%                             & $+1.81$                                   & 0\%                                 & 97\%   & \phantom{0}3\% & $-1.07$ & $+1.97$ \\
            Global financial crisis & 72.2\%                             & $+0.74$                                   & 0\%                                 & 94\%   & \phantom{0}6\% & $-1.58$ & $+0.74$ \\
            Euro sovereign 2011     & 100.0\%                            & $+3.46$                                   & 12\%                                & 88\%   & \phantom{0}0\% & $-0.61$ & $+3.69$ \\
            Covid crash             & 100.0\%                            & $+2.25$                                   & 0\%                                 & 100\%  & \phantom{0}0\% & $-0.88$ & $+2.76$ \\
            Inflation 2022          & \phantom{0}0.0\%                   & $-1.82$                                   & 0\%                                 & 33\%   & 67\%           & $-0.93$ & $-1.90$ \\
            \bottomrule
        \end{tabular}
    \end{adjustbox}
\end{table}

The crisis evidence links the regime labels to the allocation question. The disinflationary equity sell-offs sit in the normal regime, where the bond hedge stays intact, while the inflation-led sell-offs (1994, 2022) sit in the stressed regime, where both legs fall. Disinflationary sell-offs therefore favour bond exposure, while inflation-led sell-offs call for lower exposure to both risky legs.

\paragraph{Feature separation.}
Feature-separation diagnostics identify the inputs that define the teacher states. The
\Bindep{} equity labels load most on medium-horizon Sortino ratios and return trends; the
bond labels load most on Sharpe ratios and downside-risk measures. In the \Acoup{} teacher,
equity trend distinguishes calm months, and bond trend and bond Sortino measures distinguish
stressed months. Full rankings are reported in Appendices~\ref{app:teacher-independent}
and~\ref{app:teacher-coupled}.


\section{Student: forecasting the next-month regime}
\label{sec:student}

The student stage turns realised teacher labels into out-of-sample forecast probabilities.
At the end of month \(t\), the predictors \(X_t\) are observed and the target is the teacher
label in month \(t+1\). The \Bindep{} student estimates two binary favourable-state
probabilities, one for equity and one for bonds:
\begin{equation}
    \hat p^a_{t+1|t}
    =
    \Pr\!\left(S^a_{t+1}=1\mid X_t\right),
    \qquad a\in\{\mathrm{eq},\mathrm{bd}\}.
    \label{eq:independent-student-probability}
\end{equation}

The \Acoup{} student estimates the corresponding three-class probability vector over calm,
normal, and stressed joint regimes. The student models use regularised logistic regression
and random forests; Appendix~\ref{app:student} and Appendix~\ref{app:student-coupled}
report the predictor designs, validation protocol, model grids, and full forecast
diagnostics.

\paragraph{Forecast quality.}
Table~\ref{tab:forecast} reports out-of-sample forecast performance. The \Bindep{} forecasts
are evaluated as two binary favourable-state predictions. The \Acoup{} forecast is evaluated
as a three-class probability forecast over the joint stock--bond regimes. AUC values are
therefore compared within panels, not as direct cross-panel levels: the \Bindep{} tasks are
binary per-leg forecasts, while the \Acoup{} task is a three-class joint-regime forecast. The
allocation section tests whether these probability forecasts improve net portfolio outcomes.

\begin{table}[H]
    \centering
    \caption{Out-of-sample student forecast quality. The \Bindep{} rows report binary
        diagnostics for the equity and bond favourable-state forecasts. The full independent
        predictor-design results are in Table~\ref{tab:student-oos-prediction-summary}. The
        \Acoup{} rows report diagnostics for the joint three-class forecast; AUC is the macro
        one-vs-rest average, and Brier and log loss are reported in their multiclass forms.}
    \label{tab:forecast}
    \footnotesize
    \setlength{\tabcolsep}{5.0pt}
    \renewcommand{\arraystretch}{1.03}
    \begin{adjustbox}{center,max width=1.12\textwidth}
        \begin{tabular}{@{}llllcccc@{}}
            \toprule
            Approach  & Task   & Model               & OOS sample & AUC   & Bal.\ acc. & Brier & Log loss \\
            \midrule
            \Bindep{} & Equity & Logistic regression & 1996--2024 & 0.929 & 0.846      & 0.088 & 0.296    \\
            \Bindep{} & Equity & Random forest       & 1996--2024 & 0.903 & 0.831      & 0.107 & 0.361    \\
            \Bindep{} & Bond   & Logistic regression & 1996--2024 & 0.892 & 0.831      & 0.128 & 0.462    \\
            \Bindep{} & Bond   & Random forest       & 1996--2024 & 0.873 & 0.800      & 0.147 & 0.471    \\
            \midrule
            \Acoup{}  & Joint  & Logistic regression & 1996--2024 & 0.649 & 0.546      & 0.515 & 1.188    \\
            \Acoup{}  & Joint  & Random forest       & 1996--2024 & 0.648 & 0.479      & 0.398 & 0.938    \\
            \bottomrule
        \end{tabular}
    \end{adjustbox}
\end{table}

The \Bindep{} results favour logistic regression on the headline binary metrics, while the
\Acoup{} results show a trade-off between class accuracy and probabilistic scoring. Full
predictor-design comparisons and probability-path diagnostics are reported in
Appendices~\ref{app:student-regime-prediction-diagnostics} and~\ref{app:student-coupled}.

\paragraph{Predictor relevance.}
Predictor-relevance diagnostics are reported in Appendices~\ref{app:student-regime-prediction-diagnostics}
and~\ref{app:student-coupled}. Equity-regime forecasts rely more on equity cross-section,
industry, and style information. Bond-regime forecasts use a broader mix of equity,
Treasury, and credit information. The \Acoup{} forecast draws on a more dispersed set of
regime-context, commodity, Treasury, and macro-financial blocks, consistent with a joint
stock--bond target.

\section{Regime-aware allocation}
\label{sec:alloc}

The allocation stage assesses whether student forecast probabilities enhance a monthly equity-bond-cash portfolio after accounting for transaction costs. At the end of each month $t$, these forecasts are mapped into regime-conditional return moments, which then serve as inputs for a mean-variance optimization to determine target portfolio weights. We evaluate the realized returns net of one-way transaction costs, comparing the performance against a monthly rebalanced 60/40 and additional benchmarks.

For the \Bindep{} allocator, \(\hat p^{a}_{t+1|t}\) is the end-of-month-\(t\) probability
that asset \(a\in\{\mathrm{eq},\mathrm{bd}\}\) is favourable in month \(t+1\). State-specific
means and variances are estimated from past returns assigned to each state and shrunk toward
pre-out-of-sample unconditional moments with weight \(n^a_{s,t}/(n^a_{s,t}+36)\). The
probability-mixture mean is
\begin{equation}
    \hat\mu^a_t
    =
    (1-\hat p^a_{t+1|t})\hat\mu^a_{0,t}
    +
    \hat p^a_{t+1|t}\hat\mu^a_{1,t}.
    \label{eq:independent-allocation-mean}
\end{equation}
The variance uses the corresponding second-moment mixture. The covariance matrix uses an
expanding stock--bond correlation estimate shrunk toward the pre-prediction stock--bond
correlation. Appendix~\ref{app:allocation} reports the full probability-to-moment and
realised net-return formulas.

For the \Bindep{} allocator, the monthly portfolio is long-only in equity, bonds, and cash and solves
\begin{equation}
    w_t
    =
    \arg\max_{w\in\mathcal W}
    \left\{
    w^{\top}\hat\mu_t
    -
    \gamma w^{\top}\hat\Sigma_t w
    -
    \tau c \lVert w-w_{t-1}\rVert_1
    \right\},
    \label{eq:mv}
\end{equation}
where \(\mathcal W\) is the long-only equity--bond--cash simplex, \(\gamma\) is risk aversion,
\(c\) is the one-way transaction cost, and \(\tau\) scales turnover control.
Appendix~\ref{app:alloc-independent} reports the independent grid, selected-strategy
settings, additional benchmarks, and parameter sweeps.

The \Acoup{} allocator uses the predicted probabilities of calm, normal, and stressed joint
regimes in the same portfolio objective. Its conditional moments are formed as a probability
mixture over the three joint regimes, estimated from past returns assigned to each joint
regime using the same shrinkage, probability smoothing, and covariance construction as the
\Bindep{} allocator. The two allocators are otherwise identical: they share the long-only
equity--bond--cash universe, the $0.02$ weight grid, the mean--variance objective~\eqref{eq:mv},
the identical risk-aversion, turnover-penalty, transaction-cost, and probability-smoothing grids, and
the $10$~bps headline one-way cost, and they are evaluated on the same out-of-sample window
(1996-02 to 2024-10). They differ only in the regime representation that supplies the
conditional moments, which isolates the value of the representation itself.
Appendix~\ref{app:alloc-coupled} reports the coupled allocation details,
benchmark rules, cost sensitivity, and parameter sweeps. The headline results for both
approaches are evaluated against the 60/40 benchmark.

\subsection{Allocation trade-off}
\label{sec:alloc-results}
\label{subsec:results-dynamic-allocation}

The allocation results show that regime forecasts have different portfolio roles across the
selected dynamic strategies. Table~\ref{tab:alloc-frontier} reports three roles from each
allocation grid: a defensive allocation, a low-turnover allocation, and a high-return
allocation. Both approaches define these roles mechanically on the same headline-cost
grid: the defensive role maximises the Sharpe ratio subject to a maximum-drawdown floor, the
low-turnover role minimises annual turnover subject to a Sharpe floor, and the high-return
role maximises annualised excess return subject to a maximum-drawdown floor
(Appendices~\ref{app:independent-model-selection-protocol}
and~\ref{app:coupled-model-selection-protocol}). The selected rows summarise the main
trade-off between drawdown control, trading intensity, and terminal wealth.
Appendices~\ref{app:alloc-independent} and~\ref{app:alloc-coupled} report the selection
protocol, full allocation tables, cost sensitivity, and parameter sweeps. Additional
independent benchmarks, including equity-only, bond-only, and volatility-targeted 60/40
allocations, are reported in Appendix~\ref{app:independent-dynamic-benchmarks}.

\begin{table}[H]
    \centering
    \caption{Selected allocation results, out of sample. The table reports representative dynamic
        roles and the 60/40 benchmark, which is reported once in the \Bindep{} panel. Returns are annualised excess returns net of 10~bps one-way
        transaction costs; ``Final'' indexes wealth to \(100\) at sample start. Both approaches use
        long-only equity--bond--cash weights and monthly rebalancing; the appendices report
        approach-specific moment estimates, grids, and turnover-penalty conventions.}
    \label{tab:alloc-frontier}
    \small
    \begin{adjustbox}{center,max width=1.12\textwidth}
        \begin{tabular}{@{}lllrrrrr@{}}
            \toprule
            Approach  & Role         & Strategy                              & Ann.\ exc. & Vol.   & Sharpe & Max DD    & Final \\
            \midrule
            \Bindep{} & Defensive    & Random forest, smoothed predictors    & $+4.4\%$   & 5.8\%  & 0.76   & $-13.0\%$ & 636.6 \\
            \Bindep{} & Low-turnover & Logistic, z-score + smoothed z-score  & $+4.8\%$   & 7.2\%  & 0.67   & $-11.5\%$ & 720.0 \\
            \Bindep{} & High-return  & Logistic, smoothed predictors         & $+7.6\%$   & 10.6\%  & 0.71   & $-18.9\%$ & 1513.6 \\
            \Bindep{} & Benchmark    & 60/40                                 & $+4.5\%$   & 9.6\%  & 0.47   & $-36.2\%$ & 359.4 \\
            \midrule
            \Acoup{}  & Defensive    & Random forest, prob.\ mixture ($\gamma{=}5$)  & $+3.2\%$ & 4.3\%  & 0.73   & $-14.3\%$ & 240.2 \\
            \Acoup{}  & Low-turnover & Random forest, prob.\ mixture ($\gamma{=}12$) & $+1.7\%$ & 2.7\%  & 0.64   & $-10.5\%$ & 160.8 \\
            \Acoup{}  & High-return  & Logistic, prob.\ mixture ($\gamma{=}0.25$)    & $+5.3\%$ & 11.0\% & 0.48   & $-34.8\%$ & 381.2 \\
            \bottomrule
        \end{tabular}
    \end{adjustbox}
\end{table}

Table~\ref{tab:alloc-frontier} shows that the selected strategies improve on the 60/40
benchmark along different margins. Defensive allocations mainly reduce drawdowns and raise
Sharpe ratios. Low-turnover allocations preserve part of this risk control with
less aggressive rebalancing. High-return allocations accept larger drawdowns in exchange for
greater participation in favourable regimes. The full metric set is reported in
Tables~\ref{tab:c-headline}, \ref{tab:allocation-performance-summary},
and~\ref{tab:allocation-selected-strategies}.

Figure~\ref{fig:alloc-wealth} reports the corresponding wealth and drawdown paths.

\begin{figure}[H]
    \centering
    \makebox[\textwidth][c]{%
    \begin{minipage}[t]{0.50\textwidth}
        \includegraphics[width=\linewidth]{allocation_wealth_drawdown_focused.jpg}
    \end{minipage}\hspace{0.025\textwidth}%
    \begin{minipage}[t]{0.50\textwidth}
        \includegraphics[width=\linewidth]{COUPLED__alloc_wealth_drawdown.pdf}
    \end{minipage}}
    \caption{Dynamic allocation performance, out of sample. The panels report net wealth indexes
        and drawdowns for selected dynamic allocations and the 60/40 benchmark, with wealth indexed
        to \(100\) at the start of each evaluation sample.}
    \label{fig:alloc-wealth}
\end{figure}


\subsection{Portfolio-weight behaviour}

Figure~\ref{fig:alloc-mech} illustrates the empirical allocation mechanics and the resulting trade-offs between position smoothing and turnover. As visible in the structural profiles across weight plots, the optimization behavior splits along strategic objectives:

The high-return rules frequently trigger discrete, binary switches between asset classes, hitting corner solutions where the portfolio shifts entirely into a single dominant asset class. This behavior occurs because the optimization targets maximum conditional returns, causing aggressive portfolio rebalancing and sharp spikes in rolling turnover whenever a regime shift is predicted.

Conversely, the defensive and low-turnover configurations exhibit highly continuous, smooth weight trajectories. Rather than abruptly abandoning an asset class, these strategies scale exposures gradually in response to shifting forecast probabilities.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \includegraphics[width=\linewidth,height=0.20\textheight,keepaspectratio]{allocation_mechanics_focused.jpg}
    \end{minipage}\hfill
    \begin{minipage}{0.50\textwidth}
        \includegraphics[width=\linewidth,height=0.20\textheight,keepaspectratio]{COUPLED__alloc_mechanics.pdf}
    \end{minipage}
    \caption{Portfolio weights and turnover, out of sample. The panels report equity, bond, and
        cash weights and turnover for the selected dynamic allocations.}
    \label{fig:alloc-mech}
\end{figure}

\subsection{Sensitivity}

Appendices~\ref{app:alloc-independent} and~\ref{app:alloc-coupled} report sensitivity to
risk aversion, turnover penalties, transaction costs, probability smoothing, and moment
shrinkage. The patterns are stable: higher risk aversion lowers risky exposure, smoothing
and turnover penalties reduce trading, and higher transaction costs lower net performance.

\section{Discussion and conclusion}
\label{sec:conclusion}

This paper asked whether regimes estimated from stock and bond return behaviour can be
forecast one month ahead and used in allocation. The evidence is benchmark-relative.
Regime forecasts improve selected allocation outcomes along different margins: drawdown
control, Sharpe ratio, turnover, and terminal wealth.

The first contribution is the construction of economically meaningful teacher labels. The
\Bindep{} labels separate favourable and unfavourable environments for each asset class:
favourable equity and bond states have positive Sharpe ratios, while unfavourable states have
negative Sharpe ratios. The \Acoup{} labels separate the stock--bond environment directly.
Calm and normal regimes describe periods in which at least one risky leg remains useful,
whereas stressed regimes identify months in which both equity and bond returns are adverse.
This matters because the allocation problem is not only whether equities are attractive, but
whether bonds still diversify equity risk.

The crisis-window evidence gives the clearest economic interpretation of the labels.
During the dot-com bust, the global financial crisis, the euro-sovereign episode, and the
Covid crash, bond exposure remained favourable and helped the balanced portfolio. During the
1994 bond shock and the 2022 inflation episode, bond exposure became part of the adverse
environment. The regime models therefore recover the distinction that motivated the project:
some equity sell-offs preserve the stock--bond hedge, while inflation-led sell-offs can
damage both legs at the same time.

The second contribution is the separation between regime identification and regime
forecasting. The teacher stage uses realised information to define the states. The student
stage then asks whether those states are predictable from lagged macro-financial and
regime-context variables. The out-of-sample diagnostics show that this step is feasible:
the \Bindep{} equity and bond labels are forecast with strong binary performance, and the
\Acoup{} labels retain useful information despite the harder three-class joint-regime task.
The student results show that the realised labels contain information that is partly
predictable from lagged variables.

The allocation results show where the forecasts have economic value. Defensive rules use
adverse-regime probabilities to reduce drawdowns relative to 60/40. Low-turnover and balanced
rules retain part of this risk control while limiting rebalancing. High-return rules use
favourable forecasts to keep larger risky exposure and improve terminal wealth. The evidence
therefore supports regime forecasts as allocation inputs, not as stand-alone market-timing
signals.
\appendix
\renewcommand{\thefigure}{\thesection.\arabic{figure}}
\renewcommand{\thetable}{\thesection.\arabic{table}}

\input{regime_aware_tactical_asset_allocation_appendix}

\clearpage
%\section*{References}

\begin{thebibliography}{99}

\bibitem{aydinhan2024identifying}
Ayd{\i}nhan, A.~O., Kolm, P.~N., Mulvey, J.~M., and Shu, Y. (2024).
Identifying patterns in financial markets: extending the statistical jump model for regime identification.
\emph{Annals of Operations Research}.
doi:10.1007/s10479-024-06035-z.

\bibitem{bemporad2018fitting}
Bemporad, A., Breschi, V., Piga, D., and Boyd, S.~P. (2018).
Fitting jump models.
\emph{Automatica}, 96, 11--21.
doi:10.1016/j.automatica.2018.06.022.

\bibitem{mccracken2016fredmd}
McCracken, M.~W. and Ng, S. (2016).
FRED-MD: A monthly database for macroeconomic research.
\emph{Journal of Business \& Economic Statistics}, 34(4), 574--589.
doi:10.1080/07350015.2015.1086655.

\bibitem{mccracken2021fredqd}
McCracken, M.~W. and Ng, S. (2021).
FRED-QD: A quarterly database for macroeconomic research.
\emph{Federal Reserve Bank of St. Louis Review}, 103(1), 1--44.
doi:10.20955/r.103.1-44.

\bibitem{nystrup2021feature}
Nystrup, P., Kolm, P.~N., and Lindstr{\"o}m, E. (2021).
Feature selection in jump models.
\emph{Expert Systems with Applications}, 184, 115558.
doi:10.1016/j.eswa.2021.115558.

\bibitem{oliveira2025tactical}
Oliveira, D.~C., Sandfelder, D., Fujita, A., Dong, X., and Cucuringu, M. (2025).
Tactical asset allocation with macroeconomic regime detection.
\emph{arXiv preprint arXiv:2503.11499}.

\bibitem{shu2024downside}
Shu, Y., Yu, C., and Mulvey, J.~M. (2024).
Downside risk reduction using regime-switching signals: a statistical jump model approach.
\emph{Journal of Asset Management}, 25(5), 493--507.
doi:10.1057/s41260-024-00376-x.

\bibitem{shu2024dynamic}
Shu, Y., Yu, C., and Mulvey, J.~M. (2024).
Dynamic asset allocation with asset-specific regime forecasts.
\emph{arXiv preprint arXiv:2406.09578}.

\end{thebibliography}

\end{document}

